import type { CustomPack } from '@/custom/core/types'

const defaultPack: CustomPack = {
  id: 'default',
  displayName: 'Default',
  routes: [],
  navItems: [],
  pageOverrides: {},
}

export default defaultPack
